@model IEnumerable<IsletmeTipleri>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int sayac = 1;
}

<h1>İşletme Tipleri</h1>

<a class="btn btn-success mb-3" href="/Admin/IsletmeTipi/AddIsletmeTipi">İşletme Tipi Ekle</a>
<div class="table-responsive">
    <table class="table table-striped">
        <tr>
            <th>#</th>
            <th>İşletme Tipi</th>
            <th>Güncelle</th>
            <th>Sil</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@sayac</td>
                <td>@item.Name</td>
                <td><a class="btn btn-warning" href="/Admin/IsletmeTipi/UpdateIsletmeTipi/@item.ID">Güncelle</a></td>
                <td>
                    <form asp-controller="IsletmeTipi" asp-action="DeleteIsletmeTipi" asp-route-id="@item.ID" onsubmit="return confirm('İşletme tipini silmek iştediğinize eminmisiniz')">
                        <button type="submit" class="btn btn-danger">Sil</button>
                    </form>
                </td>
            </tr>
            sayac++;
        }
    </table>
</div>